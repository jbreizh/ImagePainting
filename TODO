(done) return fileList in json

(done) improve javascript : create a function to handle interface actualisation
 
(done) return bitmapLoad in json with STATUS  ANIMATIONINDEXSTART ANIMATIONINDEXSTOP
 
(done) slider in user interface to tweet ANIMATIONINDEXSTART ANIMATIONINDEXSTOP

(done) send ANIMATIONINDEXSTART ANIMATIONINDEXSTOP for animation

() zoom canvas to fit the space
